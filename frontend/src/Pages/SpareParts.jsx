import { useEffect, useState } from "react";
import { API_URL } from "../config";
import { useNavigate } from "react-router-dom";
import { useAuth } from "../auth/AuthContext";

function SpareParts() {
  const { accessToken, hasPermission, isAuthenticated } = useAuth(); const navigate = useNavigate();
  const [parts,setParts]=useState([]); const [loading,setLoading]=useState(true); const [error,setError]=useState(""); const [deactivatingId,setDeactivatingId]=useState(null);
  const loadParts=async()=>{const response=await fetch(`${API_URL}/spare-parts/`,{headers:{Authorization:`Bearer ${accessToken}`}});if(!response.ok)throw new Error(`Failed to load spare parts: ${response.status} ${await response.text()}`);setParts(await response.json());};
  useEffect(()=>{if(!isAuthenticated||!accessToken)return;loadParts().catch(err=>setError(err.message)).finally(()=>setLoading(false));},[accessToken,isAuthenticated]);
  const deactivate=async(part)=>{if(!window.confirm(`Deactivate ${part.part_code} — ${part.name}?`))return;setDeactivatingId(part.id);setError("");try{const response=await fetch(`${API_URL}/spare-parts/${part.id}`,{method:"DELETE",headers:{Authorization:`Bearer ${accessToken}`}});if(!response.ok)throw new Error(`Failed to deactivate spare part: ${response.status} ${await response.text()}`);await loadParts();}catch(err){setError(err.message)}finally{setDeactivatingId(null)}};
  if(!isAuthenticated)return <div className="spare-list-page"><div className="spare-state-card">Please log in to view spare parts.</div></div>;
  if(!hasPermission("view_spare"))return <div className="spare-list-page"><div className="spare-state-card">You do not have permission to view spare parts.</div></div>;
  return <div className="spare-list-page"><style>{`.spare-list-page{max-width:1250px;margin:0 auto;padding:28px 24px 48px;color:#172033}.spare-list-page *{box-sizing:border-box}.spare-list-header{display:flex;justify-content:space-between;align-items:center;gap:18px;margin-bottom:24px}.spare-list-header h2{margin:0;font-size:28px}.spare-list-header p{margin:6px 0 0;color:#6b7280}.spare-add{border:0;border-radius:10px;padding:11px 16px;background:#3157b7;color:#fff;font-weight:700;cursor:pointer}.spare-alert{margin-bottom:18px;padding:13px 15px;border-radius:12px;background:#fff5f5;border:1px solid #fecaca;color:#b42318}.spare-table-card{overflow:hidden;border:1px solid #e5e9f0;border-radius:18px;background:#fff;box-shadow:0 8px 28px rgba(31,41,55,.06)}.spare-table-wrap{overflow-x:auto}.spare-table{width:100%;min-width:950px;border-collapse:collapse}.spare-table th{padding:13px 16px;text-align:left;background:#f8fafc;color:#667085;font-size:12px;text-transform:uppercase;letter-spacing:.04em}.spare-table td{padding:15px 16px;border-top:1px solid #edf0f4;vertical-align:middle;font-size:14px}.spare-table tbody tr:hover{background:#fbfcfe}.spare-primary-text{font-weight:800;color:#253b7a}.spare-secondary-text{margin-top:3px;color:#8a93a3;font-size:12px;max-width:280px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.spare-status{display:inline-flex;align-items:center;gap:7px;border-radius:999px;padding:5px 10px;font-size:12px;font-weight:700}.spare-status.active{background:#eefbf3;color:#18794e}.spare-status.inactive{background:#f2f4f7;color:#667085}.spare-status:before{content:"";width:6px;height:6px;border-radius:50%;background:currentColor}.spare-actions{display:flex;gap:8px}.spare-actions button{border:0;border-radius:8px;padding:8px 11px;font-weight:700;cursor:pointer}.spare-edit{background:#eef4ff;color:#3157b7}.spare-deactivate{background:#fff1f2;color:#c4323f}.spare-deactivate:disabled{opacity:.55;cursor:not-allowed}.spare-empty,.spare-state-card{padding:32px;text-align:center;border:1px solid #e5e9f0;border-radius:18px;background:#fff;box-shadow:0 8px 28px rgba(31,41,55,.05)}.spare-empty strong{display:block;font-size:16px}.spare-empty p{margin:7px 0 0;color:#7b8494}.spare-loading{text-align:center;padding:40px;color:#667085}@media(max-width:700px){.spare-list-page{padding:20px 14px 36px}.spare-list-header{align-items:flex-start;flex-direction:column}.spare-add{width:100%}}`}</style><div className="spare-list-header"><div><h2>Spare Parts</h2><p>Maintain the active spare-parts catalog.</p></div>{hasPermission("create_spare")&&<button className="spare-add" onClick={()=>navigate("/spare-parts/new")}>+ Add Spare Part</button>}</div>{error&&<div className="spare-alert">{error}</div>}{loading?<div className="spare-loading">Loading spare parts…</div>:parts.length===0?<div className="spare-empty"><strong>No spare parts found</strong><p>Add a spare part to start building the catalog.</p></div>:<div className="spare-table-card"><div className="spare-table-wrap"><table className="spare-table"><thead><tr><th>Part code</th><th>Name</th><th>Category</th><th>Manufacturer</th><th>Unit</th><th>Minimum stock</th><th>Status</th><th>Actions</th></tr></thead><tbody>{parts.map(part=><tr key={part.id}><td><button type="button" className="entity-link spare-primary-text" onClick={() => navigate(`/spare-parts/${part.id}`)}>{part.part_code}</button></td><td><button type="button" className="entity-link entity-link-block" onClick={() => navigate(`/spare-parts/${part.id}`)}><strong>{part.name}</strong></button>{part.description&&<div className="spare-secondary-text">{part.description}</div>}</td><td>{part.category||"—"}</td><td>{part.manufacturer||"—"}</td><td>{part.unit}</td><td>{part.minimum_stock}</td><td><span className={`spare-status ${part.is_active?"active":"inactive"}`}>{part.is_active?"Active":"Inactive"}</span></td><td><div className="spare-actions">{hasPermission("update_spare")&&<button className="spare-edit" onClick={()=>navigate(`/spare-parts/${part.id}/edit`)}>Edit</button>}{hasPermission("deactivate_spare")&&<button className="spare-deactivate" disabled={deactivatingId===part.id} onClick={()=>deactivate(part)}>{deactivatingId===part.id?"Deactivating…":"Deactivate"}</button>}</div></td></tr>)}</tbody></table></div></div>}</div>;
}
export default SpareParts;



