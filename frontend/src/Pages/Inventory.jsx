import { useEffect, useMemo, useState } from "react";
import { API_URL } from "../config";
import { useAuth } from "../auth/AuthContext";
import { useNavigate } from "react-router-dom";
const movementTypes=[{value:"RECEIPT",label:"Receipt"},{value:"ISSUE",label:"Issue"},{value:"ADJUSTMENT_IN",label:"Adjustment In"},{value:"ADJUSTMENT_OUT",label:"Adjustment Out"}];
function Inventory(){
 const {accessToken,hasPermission,isAuthenticated}=useAuth();const[inventory,setInventory]=useState([]);const[parts,setParts]=useState([]);const[lowStock,setLowStock]=useState([]);const[loading,setLoading]=useState(true);const[error,setError]=useState("");const[saving,setSaving]=useState(false);const[movement,setMovement]=useState({spare_part_id:"",movement_type:"RECEIPT",quantity:"",location:"MAIN",reference:"",notes:""});const partMap=useMemo(()=>new Map(parts.map(part=>[part.id,part])),[parts]);
 const load=async()=>{const headers={Authorization:`Bearer ${accessToken}`};const[inventoryResponse,lowResponse,partsResponse]=await Promise.all([fetch(`${API_URL}/inventory/`,{headers}),fetch(`${API_URL}/inventory/low-stock`,{headers}),fetch(`${API_URL}/spare-parts/`,{headers})]);for(const response of[inventoryResponse,lowResponse,partsResponse])if(!response.ok)throw new Error(`Failed to load inventory data: ${response.status} ${await response.text()}`);setInventory(await inventoryResponse.json());setLowStock(await lowResponse.json());setParts(await partsResponse.json())};
 useEffect(()=>{if(!isAuthenticated||!accessToken)return;Promise.resolve().then(load).catch(err=>setError(err.message)).finally(()=>setLoading(false))},[accessToken,isAuthenticated]);
 const submit=async event=>{event.preventDefault();setError("");const quantity=Number(movement.quantity);if(!movement.spare_part_id||!Number.isInteger(quantity)||quantity<=0){setError("Select a part and enter a positive whole quantity.");return}setSaving(true);try{const response=await fetch(`${API_URL}/stock-movements/`,{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${accessToken}`},body:JSON.stringify({...movement,spare_part_id:Number(movement.spare_part_id),quantity,location:movement.location.trim()||"MAIN",reference:movement.reference.trim()||null,notes:movement.notes.trim()||null})});if(!response.ok)throw new Error(`Failed to record stock movement: ${response.status} ${await response.text()}`);setMovement({spare_part_id:"",movement_type:"RECEIPT",quantity:"",location:"MAIN",reference:"",notes:""});await load()}catch(err){setError(err.message)}finally{setSaving(false)}};
 if(!isAuthenticated)return <div className="inventory-page"><div className="inventory-state">Please log in to view inventory.</div></div>;if(!hasPermission("view_inventory"))return <div className="inventory-page"><div className="inventory-state">You do not have permission to view inventory.</div></div>;
 return <div className="inventory-page"><style>{`.inventory-page{max-width:1250px;margin:0 auto;padding:28px 24px 48px;color:#172033}.inventory-page *{box-sizing:border-box}.inventory-head{margin-bottom:24px}.inventory-head h2{margin:0;font-size:28px}.inventory-head p{margin:6px 0 0;color:#6b7280}.inventory-alert{margin-bottom:18px;padding:13px 15px;border-radius:12px;background:#fff5f5;border:1px solid #fecaca;color:#b42318}.inventory-summary{margin-bottom:18px}.inventory-health{display:flex;flex-direction:column;gap:5px;padding:16px 18px;border-radius:14px;border:1px solid #dfe7f2;background:#f7faff}.inventory-health.low{background:#fff9eb;border-color:#f4dfaa}.inventory-health strong{font-size:14px}.inventory-health span{font-size:13px;color:#667085}.inventory-form,.inventory-table-card{background:#fff;border:1px solid #e5e9f0;border-radius:18px;box-shadow:0 8px 28px rgba(31,41,55,.06)}.inventory-form{margin-bottom:22px;overflow:hidden}.inventory-form-head{padding:22px 24px;border-bottom:1px solid #edf0f4}.inventory-form-head h3{margin:0;font-size:17px}.inventory-form-head p{margin:5px 0 0;color:#7b8494;font-size:13px}.inventory-grid{padding:22px 24px;display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:18px}.inventory-field{display:flex;flex-direction:column;gap:7px}.inventory-field label{font-size:13px;font-weight:700;color:#374151}.inventory-field input,.inventory-field select{width:100%;border:1px solid #d9dee8;border-radius:10px;padding:11px 12px;background:#fff;color:#172033;font:inherit;outline:none}.inventory-field input:focus,.inventory-field select:focus{border-color:#6b8fe8;box-shadow:0 0 0 3px rgba(107,143,232,.13)}.inventory-actions{display:flex;justify-content:flex-end;padding:16px 24px;background:#fafbfc;border-top:1px solid #edf0f4}.inventory-primary{border:0;border-radius:10px;padding:10px 16px;background:#3157b7;color:#fff;font-weight:700;cursor:pointer}.inventory-primary:disabled{opacity:.6;cursor:not-allowed}.inventory-table-wrap{overflow-x:auto}.inventory-table{width:100%;min-width:720px;border-collapse:collapse}.inventory-table th{padding:13px 16px;text-align:left;background:#f8fafc;color:#667085;font-size:12px;text-transform:uppercase;letter-spacing:.04em}.inventory-table td{padding:15px 16px;border-top:1px solid #edf0f4;font-size:14px}.inventory-table tbody tr:hover{background:#fbfcfe}.inventory-part{font-weight:800;color:#253b7a}.inventory-sub{margin-top:3px;color:#8a93a3;font-size:12px}.inventory-state-badge{display:inline-flex;border-radius:999px;padding:5px 10px;font-size:12px;font-weight:700}.inventory-state-badge.stock{background:#eefbf3;color:#18794e}.inventory-state-badge.low{background:#fff8df;color:#b45309}.inventory-state-badge.out{background:#fff1f2;color:#c4323f}.inventory-empty,.inventory-state{padding:32px;text-align:center;border:1px solid #e5e9f0;border-radius:18px;background:#fff;box-shadow:0 8px 28px rgba(31,41,55,.05)}.inventory-loading{text-align:center;padding:40px;color:#667085}@media(max-width:850px){.inventory-page{padding:20px 14px 36px}.inventory-grid{grid-template-columns:1fr}.inventory-actions{padding:16px}.inventory-primary{width:100%}}`}</style><div className="inventory-head"><h2>Inventory</h2><p>Monitor stock by spare part and location.</p></div>{error&&<div className="inventory-alert">{error}</div>}{loading?<div className="inventory-loading">Loading inventory…</div>:<><div className="inventory-summary">{lowStock.length?<div className="inventory-health low"><strong>{lowStock.length} item{lowStock.length===1?"":"s"} need attention</strong><span>{lowStock.map(item=>`${item.part_code}: ${item.available_quantity}/${item.minimum_stock}`).join(" · ")}</span></div>:<div className="inventory-health"><strong>Stock levels are healthy</strong><span>No active parts are at or below their minimum stock.</span></div>}</div>{hasPermission("manage_inventory")&&<form className="inventory-form" onSubmit={submit}><div className="inventory-form-head"><h3>Record stock movement</h3><p>Receiving, issuing, and adjustments update inventory immediately.</p></div><div className="inventory-grid"><div className="inventory-field"><label>Spare part</label><select value={movement.spare_part_id} onChange={e=>setMovement({...movement,spare_part_id:e.target.value})} required><option value="">Select a spare part</option>{parts.map(part=><option key={part.id} value={part.id}>{part.part_code} — {part.name}</option>)}</select></div><div className="inventory-field"><label>Movement type</label><select value={movement.movement_type} onChange={e=>setMovement({...movement,movement_type:e.target.value})}>{movementTypes.map(type=><option key={type.value} value={type.value}>{type.label}</option>)}</select></div><div className="inventory-field"><label>Quantity</label><input type="number" min="1" step="1" value={movement.quantity} onChange={e=>setMovement({...movement,quantity:e.target.value})} required/></div><div className="inventory-field"><label>Location</label><input maxLength="100" value={movement.location} onChange={e=>setMovement({...movement,location:e.target.value})} required/></div><div className="inventory-field"><label>Reference</label><input maxLength="100" value={movement.reference} onChange={e=>setMovement({...movement,reference:e.target.value})}/></div><div className="inventory-field"><label>Notes</label><input value={movement.notes} onChange={e=>setMovement({...movement,notes:e.target.value})}/></div></div><div className="inventory-actions"><button className="inventory-primary" disabled={saving}>{saving?"Recording…":"Record Movement"}</button></div></form>}{inventory.length===0?<div className="inventory-empty"><strong>No inventory balances</strong><p>Record a receipt to create the first balance.</p></div>:<div className="inventory-table-card"><div className="inventory-table-wrap"><table className="inventory-table"><thead><tr><th>Part</th><th>Location</th><th>Current quantity</th><th>Minimum stock</th><th>State</th></tr></thead><tbody>{inventory.map(row=>{const part=partMap.get(row.spare_part_id);const low=part&&row.quantity<=part.minimum_stock;return <tr key={row.id}><td><button type="button" className="entity-link inventory-part" onClick={() => part && navigate(`/spare-parts/${part.id}`)}>{part?part.part_code:`Part #${row.spare_part_id}`}</button><div className="inventory-sub">{part?.name||"Catalog item unavailable"}</div></td><td>{row.location}</td><td><strong>{row.quantity}</strong></td><td>{part?.minimum_stock??"—"}</td><td><span className={`inventory-state-badge ${row.quantity===0?"out":low?"low":"stock"}`}>{row.quantity===0?"Out of stock":low?"Low stock":"In stock"}</span></td></tr>})}</tbody></table></div></div>}</>}</div>;
}
export default Inventory;




